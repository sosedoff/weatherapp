module OpenWeather
  module Forecast
    module Types
      # We would define our concrete types for the forecasting responses
      #
      # class Entity
      #   def initialize(attrs = {})
      #     # assign all matching attributes
      #   end
      # end

      # class Current
      #   attr_reader :id, :timezone, :coord, :weather, :main, :visibility, :wind

      #   def initialize(attrs)
      #     @id = attrs
      #   end
      # end
    end
  end
end
